package com.equipmentverleih.enums;

public enum ErrorNumber {
    NO_ERROR,
    EMAIL_ALREADY_IN_USE,
    INVALID_DATA,
    INVALID_CREDENTIALS,
    NOT_AUTHORIZED,
    ID_NOT_FOUND,
    REPO_ERROR
}
